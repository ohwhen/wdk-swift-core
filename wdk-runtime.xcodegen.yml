# WDK Swift Core Runtime Integration
# 
# Include this file in your project.yml to integrate WDK runtime:
#   include:
#     - path/to/wdk-runtime.xcodegen.yml
#
# Note: This is a template. For a working example, see:
# /Users/claudiovilasboas/Desktop/Tether/wdk-starter-swift/project.yml
#
# You'll need to:
# 1. Copy BareKit.xcframework to your frameworks/ directory
# 2. Create an addons/ directory for generated xcframeworks
# 3. Set up pre-actions to run bare-link and bare-pack
# 4. Reference this package: WdkSwiftCore (path: ../wdk-swift-core)

# This file provides a template for integrating WDK runtime into your iOS app.
# The actual integration requires the following structure:

# targets:
#   YOUR_TARGET_NAME:
#     dependencies:
#       - framework: frameworks/BareKit.xcframework
#       - package: WdkSwiftCore
#       - package: BareKit
#       # 17 bare addons (generated by bare-link, listed in addons/addons.yml)
#     
#     sources:
#       - path: YourAppSource
#       - path: wdk-worklet.mobile.bundle
#         optional: true
#         buildPhase: resources
#     
#     scheme:
#       preActions:
#         - name: Link Addons
#           script: |
#             PATH="${PATH}" "${PWD}/../wdk-swift-core/WorkletSource/pear-wrk-wdk-jsonrpc/node_modules/.bin/bare-link" \
#               --preset ios \
#               --out "${PWD}/addons" \
#               "${PWD}/../wdk-swift-core/WorkletSource/pear-wrk-wdk-jsonrpc"
#         - name: Pack Bundle
#           script: |
#             PATH="${PATH}" "${PWD}/../wdk-swift-core/WorkletSource/pear-wrk-wdk-jsonrpc/node_modules/.bin/bare-pack" \
#               --preset ios \
#               --base "${PWD}/../wdk-swift-core/WorkletSource/pear-wrk-wdk-jsonrpc" \
#               --imports "${PWD}/../wdk-swift-core/WorkletSource/pear-wrk-wdk-jsonrpc/pack.imports.json" \
#               --out "${PWD}/wdk-worklet.mobile.bundle" \
#               "${PWD}/../wdk-swift-core/WorkletSource/pear-wrk-wdk-jsonrpc/src/wdk-worklet.js"

# For a complete working example, refer to wdk-starter-swift
